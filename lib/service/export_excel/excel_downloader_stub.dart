import 'dart:typed_data';

Future<void> downloadExcelFileImpl({
  required Uint8List bytes,
  required String fileName,
}) async {
  throw UnsupportedError('Download Excel browser hanya tersedia di Web');
}
